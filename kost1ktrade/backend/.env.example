# Example Environment Variables

# This is the connection string for your PostgreSQL database.
# Replace the user, password, host, port, and database name with your actual credentials.
# Format: postgresql+psycopg2://<user>:<password>@<host>:<port>/<db_name>
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/kost1k_trade_db
